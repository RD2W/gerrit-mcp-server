# Gerrit MCP Server — Environment Variables
# Copy this file to config/.env and fill in your values.

# Gerrit instance URL (overrides gerrit.base_url in config.toml)
# GERRIT_URL=https://gerrit.example.com

# Auth: http_basic mode
# GERRIT_USERNAME=your-username
# GERRIT_AUTH_TOKEN=your-http-password

# Auth: bearer mode
# GERRIT_TOKEN=your-bearer-token

# TLS: custom CA certificate
# GERRIT_CA_CERT=/path/to/ca.pem
# SSL_CERT_FILE=/path/to/ca.pem
# SSL_CERT_DIR=/etc/ssl/certs

# TLS: disable verification (not recommended for production)
# GERRIT_VERIFY_SSL=false

# Logging level
# RUST_LOG=debug
